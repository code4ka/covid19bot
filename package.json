{
  "name": "covid19bot",
  "version": "0.1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "clean": "rimraff dist/*"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "typescript": "^3.9.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "ts-node-dev": "^1.0.0-pre.49"
  }
}
